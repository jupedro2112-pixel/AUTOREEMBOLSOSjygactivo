# Réplica 2026-08-27 — IA de comprobantes + CBU ajeno en hgcash + retry de saldo al pagar

Paquete para replicar en los proyectos hermanos (mismo código base: vipcargas,
girox, etc.) los fixes #124, #125 y #126 del WORKLOG de AUTOREEMBOLSOS.

Archivos tocados: `server.js`, `src/services/comprobanteAiService.js`,
`src/models/Comprobante.js`. **NO** incluye el cambio de nombre de la PWA
(manifest/index.html), que es específico de cada proyecto.

---

## Opción A — aplicar el patch (recomendado si el repo hermano no divergió)

```bash
# en el repo hermano, rama main limpia
curl -sL https://raw.githubusercontent.com/jupedro2112-pixel/AUTOREEMBOLSOSjygactivo/main/docs/replicas/2026-08-27-comprobantes-hgcash-retiros.patch -o /tmp/rep.patch
git apply --check /tmp/rep.patch && git apply /tmp/rep.patch
node --check server.js && node --check src/services/comprobanteAiService.js && node --check src/models/Comprobante.js
```

Si `git apply --check` falla por contexto distinto, ir a la Opción B (o pedirle
al asistente: *"aplicá este patch a mano, hunk por hunk, buscando las funciones
por nombre"*).

## Opción B — a mano (prompt para el asistente del otro repo)

> Replicá estos 3 cambios. El código de referencia está en el repo
> `jupedro2112-pixel/AUTOREEMBOLSOSjygactivo` (WORKLOG #124, #125, #126 y el patch
> `docs/replicas/2026-08-27-comprobantes-hgcash-retiros.patch`).

### 1. `src/services/comprobanteAiService.js` — REEMPLAZAR EL ARCHIVO ENTERO

Copiar tal cual desde
`https://raw.githubusercontent.com/jupedro2112-pixel/AUTOREEMBOLSOSjygactivo/main/src/services/comprobanteAiService.js`.

Qué trae: modelo default `claude-opus-5` (env `COMPROBANTE_AI_MODEL` lo
pisa — **revisar en EB/SSM que no quede en haiku**), salida estructurada
`output_config.format = json_schema`, prompt en `system` con reglas CBU (22
dígitos) / CUIT (11, XX-XXXXXXXX-X) / alias (palabras con puntos) ≠ N° de
operación, campos nuevos `numero_operacion_etiqueta` y `hora`, refusal-fallback
con reintento sin beta ante 400, timeout 90 s. Exporta lo mismo de siempre:
`{ isEnabled, analyzeComprobante, getModel }`; el resultado suma
`operationLabel` y `paymentTime`.

### 2. `src/models/Comprobante.js` — 3 campos + 1 enum

```js
  operationNumber: { type: String, default: null, trim: true },
  // Etiqueta impresa junto al N° de operación ("Referencia", "ID de transacción"…) — #126
  operationLabel: { type: String, default: null, trim: true },
  // N° de operación que la IA devolvió pero se DESCARTÓ por parecer CBU/CUIT/alias (#126, auditoría)
  operationNumberRejected: { type: String, default: null, trim: true },
  ...
  paymentDate: { type: String, default: null, trim: true },  // fecha tal como figura
  paymentTime: { type: String, default: null, trim: true },  // hora tal como figura (#126)
```

En `bankMatchStatus.enum` agregar `'other_cbu'` (#125).

### 3. `server.js` — 4 zonas (buscar por nombre de función)

**3a. `_deductChipsAtConfirm` (#124)** — paso 1 "Saldo actual del cliente":
cambiar `jugayganaMovements.getUserBalance(payout.username)` por
`jugayganaMovements.getUserBalanceWithRetry(payout.username)` y en el
`if (avail === null)` incluir la causa:

```js
    const why = jugaygana.errToString((balRes && balRes.error) || 'sin detalle');
    await PendingPayout.updateOne({ id: payout.id }, { $set: { status: 'failed', error: 'No se pudo leer el saldo para descontar: ' + why } });
    await _emitAdminOnlyChatNote(payout.userId, payout.username, `⚠️ No se pudo leer el saldo del cliente en JUGAYGANA para descontar $${amt.toLocaleString('es-AR')} (3 intentos: ${why}). No se descontó nada. Reintentá el pago en unos minutos.`);
    return { ok: false, error: 'JUGAYGANA no respondió al leer el saldo del cliente (3 intentos). No se descontó nada. Reintentá en unos minutos.' };
```

**3b. `getHgcashConfig` (#125)** — antes del `return merged;`:

```js
  const own = [merged.cbu];
  merged.ownAlias = '';
  try {
    const c = await getConfig('cbu', null);
    if (c && c.number) own.push(c.number);
    if (c && c.alias) merged.ownAlias = _normAlias(c.alias);
  } catch (_) {}
  merged.ownCbus = own.map(_digits).filter(d => d.length >= 6);
```

**3c. Helpers nuevos (#125)** — pegar ANTES de `_destConsistentOk`, y
reemplazar `_destConsistentOk` por la versión nueva:

```js
function _cbuSameAccount(a, b) {
  const da = _digits(a), db = _digits(b);
  const n = Math.min(da.length, db.length);
  if (n < 6) return null;
  return da.slice(-n) === db.slice(-n);
}
function _ownCbusFor(cfg, movement) {
  const list = [...((cfg && cfg.ownCbus) || [])];
  if (movement && movement.toCBU) list.push(_digits(movement.toCBU));
  return list.filter(d => d && d.length >= 6);
}
function _normAlias(s) {
  const a = String(s || '').trim().toLowerCase().replace(/\s+/g, '');
  return /^[a-z0-9.\-_]{6,20}$/.test(a) ? a : '';
}
// Devuelve un texto ("…1212" o 'alias "xyz"') si el destino del comprobante NO es nuestro; null si es nuestro o no se puede juzgar.
function _comprobanteForeignCbu(comprobante, cfg, movement) {
  const rawDest = comprobante && comprobante.destCbu;
  const dest = _digits(rawDest);
  if (dest.length >= 6) {
    const own = _ownCbusFor(cfg, movement);
    if (!own.length) return null;
    for (const o of own) { if (_cbuSameAccount(dest, o) === true) return null; }
    return '…' + dest.slice(-4);
  }
  const alias = _normAlias(rawDest);
  const ownAlias = cfg && cfg.ownAlias;
  if (!alias || !ownAlias) return null;
  return alias === ownAlias ? null : `alias "${alias}"`;
}
function _destConsistentOk(comprobante, movement, cfg) {
  if (!comprobante.destHolder && !comprobante.destCbu) return true;
  if (_comprobanteForeignCbu(comprobante, cfg, movement)) return false; // el CBU manda; el titular NO alcanza
  if (movement) {
    if (comprobante.destCbu && movement.toCBU && _cbuSameAccount(comprobante.destCbu, movement.toCBU) === true) return true;
    if (comprobante.destHolder && movement.toName && _nameMatch(comprobante.destHolder, movement.toName)) return true;
  }
  if (_comprobanteToOurBank(comprobante, cfg)) return true;
  return false;
}
```

Y en `hgcashMatchFromComprobante`, justo después del guard
`if (!comprobante || !comprobante.isComprobante || comprobante.autoCharged) return;`:

```js
    const foreign = _comprobanteForeignCbu(comprobante, cfg, null);
    if (foreign) {
      const ours = [...(cfg.ownCbus || []).map(d => '…' + d.slice(-4)), cfg.ownAlias ? `alias "${cfg.ownAlias}"` : ''].filter(Boolean).join(' / ') || '?';
      try { await Comprobante.updateOne({ id: comprobante.id }, { $set: { bankMatchStatus: 'other_cbu', toApiBank: false } }); } catch (_) {}
      await _emitAdminOnlyChatNote(comprobante.userId, comprobante.username,
        `🏦 ⛔ Este comprobante va a OTRO CBU/alias (${foreign}), no al de este proyecto (${ours}). NO se auto-carga aunque haya una transferencia por el mismo monto. Verificá con el cliente a dónde transfirió antes de cargar.`);
      logger.info(`[hgcash] comprobante ${comprobante.id} de ${comprobante.username}: destino ajeno ${foreign} (propios: ${ours}) — excluido del match`);
      return;
    }
```

**3d. `analyzeComprobanteFromMessage` (#126)** — ver el hunk exacto en el
patch. Resumen de lo que cambia dentro de la función:

- Tras calcular `_destDig`/`_origDig`/`_rawOp`, agregar
  `const _opDig = _digits(_rawOp);`,
  `_badLabel = /\b(cbu|cvu|cuit|cuil|alias|cuenta|tarjeta|cliente|dni)\b/i.test(String(result.operationLabel || ''))`,
  `_partOfCbu = _opDig.length >= 8 && ((_destDig && _destDig.includes(_opDig)) || (_origDig && _origDig.includes(_opDig)))`,
  `let opRejected = null;` y sumar `|| _badLabel || _partOfCbu` a la condición
  que vacía `opKey`. Cuando se vacía (por cualquier motivo, incluido `< 6`):
  `opRejected = _rawOp; result.operationNumber = null;`.
- Huella de fallback: exigir `result.paymentDate` y sumar la hora:
  ```js
  const _comboByData = !dedupeKey && result.amount && (result.originHolder || result.originCbu) && result.paymentDate;
  if (_comboByData) dedupeKey = _normComprobanteKey(`${result.amount}|${result.originHolder || ''}|${result.originCbu || ''}|${result.paymentDate}|${result.paymentTime || ''}`);
  ```
- En `base` agregar `operationLabel: result.operationLabel || null,
  operationNumberRejected: opRejected || null, paymentTime: result.paymentTime || null`.
- Aviso de duplicado de OTRO usuario en 3 niveles (`_byImage` / `_byOpNumber =
  !_byImage && !!opKey && original.dedupeKey === dedupeKey` / solo datos →
  "⚠️ POSIBLE comprobante repetido… compará a ojo"). Texto exacto en el patch.

---

## Después del deploy (checklist)

1. Panel → config hgcash: **CBU propio cargado**; panel → CBU del cliente:
   **número y alias cargados** (sin eso el guard de CBU ajeno no puede juzgar y
   se comporta como antes).
2. EB/SSM: `COMPROBANTE_AI_MODEL` ausente o = `claude-opus-5` (o `claude-sonnet-5`).
3. Mandar 3 comprobantes de prueba: Mercado Pago con N° de operación + CBU
   visible (el N° NO debe ser el CBU); uno al CBU de OTRO proyecto (nota ⛔ y
   sin carga); el mismo comprobante desde otro usuario (🚨 por imagen idéntica).
4. Log: buscar `[comprobante-ai]` (400 del schema/beta) y `[comprobante] N° de
   operación descartado`.
5. Documentar en el WORKLOG del repo hermano.
